- Verify token.
  - If 
    - true, don't remove the token in the local storage.
    - false, remove the token in the local storage.
  - No matter the result, wait for the checking and ONLY AFTER THAT continue with the remaining API calls.
- Create a /team page, to mention every person who collaborated with the website, directly and indirectly.
  - With photos hosted on Google Drive.
- Implement skeletons.
- Add a custom loading with the logo and a white background while checking the token of the user.